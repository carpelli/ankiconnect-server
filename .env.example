# Example Environment Configuration for Lightweight AnkiConnect
# Copy this file to .env and modify the values as needed

# Server settings
ANKICONNECT_HOST=127.0.0.1
ANKICONNECT_PORT=8765

# API key for authentication (keep this secret!)
# If not set, no authentication is required
ANKICONNECT_API_KEY=your-secret-api-key-here

# CORS origins (comma-separated)
ANKICONNECT_CORS_ORIGINS=http://localhost,http://127.0.0.1:3000

# Anki collection path (auto-detected if not set)
# ANKICONNECT_COLLECTION_PATH=/path/to/collection.anki2

# Debug mode
ANKICONNECT_DEBUG=false

# Usage:
# 1. Copy this file to .env
# 2. Set your API key
# 3. Adjust other settings as needed
# 4. Never commit .env to git!
